{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm run build -w packages/core",
    "test": "npm test -w packages/core",
    "test:watch": "npm run test:watch -w packages/core",
    "typecheck": "npm run typecheck -w packages/core",
    "lint": "npm run lint -w packages/core",
    "lint:fix": "npm run lint:fix -w packages/core",
    "format": "npm run format -w packages/core",
    "format:fix": "npm run format:fix -w packages/core",
    "ci": "npm run ci -w packages/core"
  }
}
