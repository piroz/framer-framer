name: "Release: vue"

on:
  push:
    tags:
      - "vue-v[0-9]*"

concurrency:
  group: release-vue
  cancel-in-progress: false

jobs:
  release:
    uses: ./.github/workflows/release-publish.yml
    with:
      package-dir: packages/vue
    secrets: inherit
